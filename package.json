{
  "name": "drift-monorepo",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "private": true,
  "scripts": {
    "build": "cd npm && bun run build",
    "test": "cd npm && bun run test"
  },
  "workspaces": [
    "npm"
  ]
}